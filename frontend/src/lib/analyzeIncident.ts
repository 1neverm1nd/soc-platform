// Client-side helper placeholder — actual analysis goes through backend tRPC
export async function analyzeIncident(_incidentId: number): Promise<void> {
  // Analysis is triggered through the backend service
}
